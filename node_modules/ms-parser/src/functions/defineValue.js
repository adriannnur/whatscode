module.exports = (letter = String, text = String, ms = Number, maxValue = Number, number = 0) => {
    return {
        letter: letter,
        text: text,
        ms: ms,
        maxValue: maxValue,
        number: number
    }
}